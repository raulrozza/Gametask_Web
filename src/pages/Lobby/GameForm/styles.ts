import styled from 'styled-components';

export const Container = styled.div`
  textarea {
    min-height: 72px;
  }

  .buttons {
    display: flex;
    justify-content: space-between;

    button {
      width: 48%;
    }
  }
`;
